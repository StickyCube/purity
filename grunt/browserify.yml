dist:
  files:
    dist/purity.js: src/purity.js
  options:
    browserifyOptions:
      debug: true
      standalone: purity
    extensions:
      - '.js'
    transform:
      -
        - babelify
        - extensions:
            - '.js'
          presets:
            - es2015
